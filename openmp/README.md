# Fortran OpenMP demos

some commonly used OpenMP directives

## compile

- gfortran:
```shell
    gfortran -fopenmp <source-file> -o <exec-file>
```

- ifort:
```shell
    ifort -openmp <source-file> -o <exec-file>
```
